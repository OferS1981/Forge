{
  "name": "@forge/data",
  "version": "0.0.0",
  "private": true,
  "description": "The library: folders, saved prompts, recipes, pins and shares. Local by default, an account when there is one. Pure TypeScript, zero runtime dependencies.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'data: nothing to build, consumed as source'",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@forge/catalog": "workspace:*"
  },
  "devDependencies": {
    "@electric-sql/pglite": "^0.2.17",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
