{
  "name": "@forge/changelog",
  "version": "0.0.0",
  "private": true,
  "description": "What changed in the catalogue, and which of your saved prompts it touches. A diff of claims, not of text.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./history": "./snapshots/index.ts"
  },
  "scripts": {
    "build": "echo 'changelog: nothing to build, consumed as source'",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@forge/catalog": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
