{
  "name": "@forge/cli",
  "version": "0.0.0",
  "private": true,
  "description": "The command line, as a pure function. An argument list in, a code and two strings out. No process, no console, no exit.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "build": "echo 'cli: nothing to build, consumed as source'",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@forge/catalog": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.7.2",
    "vitest": "^2.1.8"
  }
}
